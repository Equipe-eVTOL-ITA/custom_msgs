# eVTOL ITA Custom ROS 2 Messages

Currently, there is only 1 custom message implemented:

- Gesture.msg: a vector of strings, so that an arbitrary number of hand gestures may be detected and published.